/**
 * Created by sksakthivel on 28/06/2016.
 */

/*function calculate(){
    var loanAmt = document.getElementById("loanAmt").value;
    var term = document.getElementById("term").value;
    var intRate = document.getElementById("intRate").value;
    console.log(loanAmt);
    console.log(term);
    console.log(intRate);

    var monInt = intRate/1200;
    var termMonths = term * 12;
    var np = Math.pow((1+monInt),termMonths);
    var monPayment = loanAmt * ((monInt * np)/(np-1));
    monPayment = Math.round(monPayment * 100)/100;
    var totPayment = monPayment * termMonths;
    var totInt = totPayment - loanAmt;
    console.log(monPayment);
    console.log(totPayment);
    console.log(totInt);
    alert("monthly payment "+ monPayment);
    document.getElementById("monPayment").value = monPayment;
    document.getElementById("totPayment").value = totPayment;
    document.getElementById("totInt").value = totInt;

    return false;
};*/
